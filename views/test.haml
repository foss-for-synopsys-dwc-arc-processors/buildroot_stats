%h2 Build from #{datetime_to_s(@test.date)} 
%div.tests
  = render(:haml, :'tests_list', locals: { list: [@test] })

%h3 Packages

%div.packages
  = render(:haml, :'clean_package_list', locals: { test_packages: @test.test_packages })
